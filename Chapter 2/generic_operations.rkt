#lang racket

;; We will create generic operations that will allow us to build systems that compute complex numbers
;; We may have different formats of data, we may have polar (trigonometric) data or we may have rectangular data.